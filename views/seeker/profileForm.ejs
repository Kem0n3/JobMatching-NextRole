<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <style>
        :root {
            --color-bg-page: #F1F6F9; --color-bg-content: #FFFFFF;
            --color-primary-text: #394867; --color-sidebar-bg: #FFFFFF;
            --color-sidebar-text: var(--color-primary-text);
            --color-sidebar-active-text: #14274E; --color-sidebar-active-bg: #DBE2EF;
            --color-accent-interactive: #3F72AF; --color-accent-action: #F05454;
            --primary-action-color: var(--color-accent-action); --primary-action-dark: #d94242;
            --text-color-on-primary: #ffffff; --text-color-base: var(--color-primary-text);
            --text-color-strong: #14274E; --text-color-subtle: #6c757d;
            --text-color-inverted: var(--color-primary-text);
            --text-color-link: var(--color-accent-interactive);
            --text-color-link-hover: var(--color-accent-action);
            --background-color-navbar: #FFFFFF; --border-color: #DBE2EF;
            --border-radius-sm: 6px; --border-radius-md: 10px;
            --font-family-main: 'Inter', sans-serif;
            --navbar-height: 65px; --sidebar-width: 270px;
            --shadow-sm: 0 2px 5px rgba(20, 39, 78, 0.06); --shadow-md: 0 5px 15px rgba(20, 39, 78, 0.08);
        }
    </style>
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/forms.css">
</head>
<body>
    <div class="app-shell">
        <aside class="sidebar-area">
            <%- include('../partials/sidebar') %>
        </aside>

        <div class="main-wrapper">
            <header class="navbar-area">
                <%- include('../partials/navbar') %>
            </header>

            <main class="main-content-area">
                <div class="profile-container">
                    <div class="profile-header">
                        <div class="profile-header-info">
                            <h1><%= title %></h1>
                            <p>Keep your profile up-to-date to get the best job matches.</p>
                        </div>
                        <div class="profile-header-actions">
                            <%# "View Profile" button/link REMOVED from here %>
                        </div>
                    </div>

                    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                        <div class="error-messages card">
                            <ul>
                                <% errors.forEach(function(error) { %>
                                    <li><%= error.msg %></li>
                                <% }); %>
                            </ul>
                        </div>
                    <% } %>
                    <form action="/profile" method="POST">
                        <div class="form-section">
                            <h2>Basic Information</h2>
                            <div class="form-group">
                                <label for="fullName">Full Name</label>
                                <input type="text" id="fullName" name="fullName" value="<%= locals.profileData && locals.profileData.fullName ? locals.profileData.fullName : '' %>" required>
                            </div>
                            <div class="form-group">
                                <label for="summary">Summary</label>
                                <textarea id="summary" name="summary" rows="5" placeholder="Tell recruiters a bit about your professional background and career goals..."><%= locals.profileData && locals.profileData.summary ? locals.profileData.summary : '' %></textarea>
                            </div>
                        </div>

                        <div class="form-section">
                            <h2>Skills & Preferences</h2>
                            <div class="form-group">
                                <label for="skills">Skills</label>
                                <select id="skills" name="skills" multiple="multiple" class="select2-multiple" style="width: 100%;">
                                    <% skillsList.forEach(function(skill) { %>
                                        <option value="<%= skill.id %>"
                                            <%= (locals.profileData && locals.profileData.skills && locals.profileData.skills.includes(skill.id)) ? 'selected' : '' %>>
                                            <%= skill.text %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="desiredJobTypes">Desired Job Types</label>
                                <select id="desiredJobTypes" name="desiredJobTypes" multiple="multiple" class="select2-multiple" style="width: 100%;">
                                    <% jobTypeList.forEach(function(type) { %>
                                        <option value="<%= type.id %>"
                                            <%= (locals.profileData && locals.profileData.desiredJobTypes && locals.profileData.desiredJobTypes.includes(type.id)) ? 'selected' : '' %>>
                                            <%= type.text %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="preferredLocations">Preferred Locations</label>
                                <select id="preferredLocations" name="preferredLocations" multiple="multiple" class="select2-multiple" style="width: 100%;">
                                     <% locationsList.forEach(function(location) { %>
                                        <option value="<%= location.id %>"
                                            <%= (locals.profileData && locals.profileData.preferredLocations && locals.profileData.preferredLocations.includes(location.id)) ? 'selected' : '' %>>
                                            <%= location.text %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="form-group checkbox-group">
                                <input type="checkbox" id="isWillingToRemote" name="isWillingToRemote" value="true"
                                    <%= (locals.profileData && locals.profileData.isWillingToRemote) ? 'checked' : '' %>>
                                <label for="isWillingToRemote">Willing to work remotely</label>
                            </div>
                        </div>

                        <div class="form-section">
                            <h2>Category Experience</h2>
                            <div id="categoryExperienceContainer">
                                <% const experiences = (locals.profileData && locals.profileData.categoryExperience && locals.profileData.categoryExperience.length > 0) ? locals.profileData.categoryExperience : [{}]; %>
                                <% experiences.forEach(function(exp, index) { %>
                                    <div class="dynamic-entry category-experience-entry">
                                        <% if (experiences.length > 1 || (exp.category_id || exp.years)) { %>
                                             <button type="button" class="remove-btn remove-category-exp" title="Remove Experience Category">Remove</button>
                                        <% } %>
                                        <div class="form-group">
                                            <label for="category_id_<%= index %>">Category</label>
                                            <select id="category_id_<%= index %>" name="categoryExperience[<%= index %>][category_id]" class="select2-single category-select" style="width: 100%;">
                                                <option value="">-- Select Category --</option>
                                                <% broaderCategoriesList.forEach(function(cat) { %>
                                                    <option value="<%= cat.id %>" <%= (exp && exp.category_id === cat.id) ? 'selected' : '' %>><%= cat.text %></option>
                                                <% }); %>
                                            </select>
                                        </div>
                                        <div class="form-group">
                                            <label for="years_<%= index %>">Years in this category</label>
                                            <input type="number" id="years_<%= index %>" name="categoryExperience[<%= index %>][years]" min="0" step="0.5" value="<%= (exp && exp.years !== undefined) ? exp.years : '' %>" placeholder="e.g., 3.5">
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                            <button type="button" id="addCategoryExperienceBtn" class="add-btn">Add Experience Category</button>
                        </div>

                        <div class="form-section">
                            <h2>Education</h2>
                            <div class="form-group">
                                <label for="degreeLevel">Highest Degree Level</label>
                                <select id="degreeLevel" name="degreeLevel" class="select2-single" style="width: 100%;" required>
                                    <option value="">-- Select Degree --</option>
                                    <% degreeLevelsList.forEach(function(degree) { %>
                                        <option value="<%= degree.id %>"
                                            <%= (locals.profileData && locals.profileData.degreeLevel === degree.id) ? 'selected' : '' %>>
                                            <%= degree.text %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="fieldOfStudy">Field of Study</label>
                                <select id="fieldOfStudy" name="fieldOfStudy" class="select2-single" style="width: 100%;" required>
                                    <option value="">-- Select Field of Study --</option>
                                    <% fieldsOfStudyList.forEach(function(field) { %>
                                        <option value="<%= field.id %>"
                                            <%= (locals.profileData && locals.profileData.fieldOfStudy === field.id) ? 'selected' : '' %>>
                                            <%= field.text %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                        </div>

                        <button type="submit" class="form-submit-button"><%= isEditMode ? 'Update Profile' : 'Save Profile' %></button>
                    </form>
                </div>
            </main>
        </div>

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
        <script src="/js/main.js"></script>
        <script>
            $(document).ready(function() {
                function initializeSelect2(selector) {
                    $(selector).select2({
                        placeholder: $(selector).find('option[value=""]').text() || "Select an option",
                        allowClear: true,
                        width: 'resolve'
                    });
                }
                $('.select2-multiple').select2({ placeholder: "Select options", allowClear: true, width: 'resolve' });
                $('.select2-single').each(function() { initializeSelect2(this); });

                function reindexCategoryExperience() {
                    $('#categoryExperienceContainer .category-experience-entry').each(function(index, entry) {
                        $(entry).find('select[name^="categoryExperience"]').attr('name', `categoryExperience[${index}][category_id]`).attr('id', `category_id_${index}`);
                        $(entry).find('input[name^="categoryExperience"]').attr('name', `categoryExperience[${index}][years]`).attr('id', `years_${index}`);
                        if ($('#categoryExperienceContainer .category-experience-entry').length > 1) {
                           $(entry).find('.remove-category-exp').show();
                        } else {
                           const catVal = $(entry).find('select[name$="[category_id]"]').val();
                           const yearsVal = $(entry).find('input[name$="[years]"]').val();
                           if (catVal || yearsVal) { $(entry).find('.remove-category-exp').show(); }
                           else { $(entry).find('.remove-category-exp').hide(); }
                        }
                    });
                }

                $('#addCategoryExperienceBtn').on('click', function() {
                    const container = $('#categoryExperienceContainer');
                    const entryCount = container.find('.category-experience-entry').length;
                    const newEntryHtml = `
                        <div class="dynamic-entry category-experience-entry">
                            <button type="button" class="remove-btn remove-category-exp" title="Remove Experience Category">Remove</button>
                            <div class="form-group">
                                <label for="category_id_${entryCount}">Category</label>
                                <select id="category_id_${entryCount}" name="categoryExperience[${entryCount}][category_id]" class="select2-single-new category-select" style="width: 100%;">
                                    <option value="">-- Select Category --</option>
                                    <% broaderCategoriesList.forEach(function(cat) { %>
                                        <option value="<%= cat.id %>"><%= cat.text %></option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="years_${entryCount}">Years in this category</label>
                                <input type="number" id="years_${entryCount}" name="categoryExperience[${entryCount}][years]" min="0" step="1" placeholder="e.g., 3">
                            </div>
                        </div>`;
                    container.append(newEntryHtml);
                    initializeSelect2(`#category_id_${entryCount}`);
                    reindexCategoryExperience();
                });

                $('#categoryExperienceContainer').on('click', '.remove-category-exp', function() {
                    $(this).closest('.dynamic-entry').remove();
                    reindexCategoryExperience();
                });
                reindexCategoryExperience();
            });
        </script>
        <style>
            
            .dynamic-entry {
                border: 1px solid var(--border-color);
                padding: 20px;
                margin-bottom: 20px;
                border-radius: var(--border-radius-sm);
                background-color: var(--color-bg-page);
                position: relative;
            }
            .remove-btn {
                position: absolute;
                top: 15px;
                right: 15px;
                background-color: transparent;
                color: var(--text-color-subtle);
                border: 1px solid var(--border-color);
                padding: 4px 10px;
                border-radius: var(--border-radius-sm);
                cursor: pointer;
                font-size: 0.8em;
                transition: background-color 0.2s ease, color 0.2s ease;
            }
            .remove-btn:hover {
                background-color: var(--color-accent-action);
                color: white;
                border-color: var(--color-accent-action);
            }
            .add-btn {
                background-color: transparent;
                color: var(--color-accent-interactive);
                border: 1px solid var(--color-accent-interactive);
                padding: 8px 15px;
                border-radius: var(--border-radius-sm);
                cursor: pointer;
                font-size: 0.9em;
                font-weight: 500;
                transition: background-color 0.2s ease, color 0.2s ease;
            }
            .add-btn:hover {
                background-color: var(--color-accent-interactive);
                color: white;
            }
            .error-messages {
                border: 1px solid var(--danger-color);
                background-color: #f8d7da;
                color: #721c24;
                padding: 15px;
                border-radius: var(--border-radius-sm);
            }
            .error-messages ul { margin: 0; padding-left: 20px; }
        </style>
    </body>
</html>