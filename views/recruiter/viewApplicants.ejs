<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-primary: #2563eb; --color-primary-dark: #1d4ed8; --color-primary-light: #dbeafe;
            --color-text-strong: #111827; --color-text-base: #374151; --color-text-subtle: #6b7280;
            --color-text-on-primary: #ffffff; --color-bg-page: #f9fafb; --color-bg-content: #ffffff;
            --color-border: #e5e7eb; --font-family-main: 'Inter', sans-serif;
            --border-radius: 0.75rem; --shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
        }
    </style>
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/view-applicants.css">
</head>
<body>
    <div class="app-shell">
        <aside class="sidebar-area">
            <%- include('../partials/sidebar') %>
        </aside>

        <div class="main-wrapper">
            <header class="navbar-area">
                <%- include('../partials/navbar') %>
            </header>

            <main class="main-content-area">
                <div class="page-header">
                    <h1>Applicants</h1>
                    <p>Viewing candidates who applied for: <strong><a href="/jobs/<%= job._id %>"><%= job.jobTitle %></a></strong></p>
                </div>

                <div class="applicants-container">
                    <% if (applications && applications.length > 0) { %>
                        <% applications.forEach(function(app) { %>
                            <div class="applicant-card">
                                <% if (app.seeker_user_id && app.seekerProfile) { %>
                                    <div class="applicant-avatar">
                                        <%= app.seekerProfile.fullName ? app.seekerProfile.fullName.charAt(0).toUpperCase() : 'S' %>
                                    </div>
                                    <div class="applicant-info">
                                        <a href="/profile/seeker/<%= app.seeker_user_id._id %>" class="applicant-name-link">
                                            <h3><%= app.seekerProfile.fullName %></h3>
                                        </a>
                                        <p class="applicant-email"><%= app.seeker_user_id.email %></p>
                                    </div>
                                    <div class="applicant-meta">
                                        <p class="application-date">Applied: <%= new Date(app.applicationDate).toLocaleDateString() %></p>
                                        <form class="status-update-form" action="/applications/status/<%= app._id %>" method="POST">
                                            <select name="newStatus" onchange="this.form.submit()">
                                                <% const statuses = ['Applied', 'Viewed', 'Under Review', 'Interviewing', 'Offered', 'Hired', 'Rejected', 'Withdrawn']; %>
                                                <% statuses.forEach(s => { %>
                                                    <option value="<%= s %>" <%= app.status === s ? 'selected' : '' %>><%= s %></option>
                                                <% }); %>
                                            </select>
                                            <button type="submit">Update</button>
                                        </form>
                                    </div>
                                <% } else { %>
                                    <div class="applicant-info">
                                        <h3>Applicant Information Missing</h3>
                                        <p class="applicant-email">Could not load profile details.</p>
                                    </div>
                                <% } %>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="no-results-card card">
                            <p>No applications received for this job yet.</p>
                        </div>
                    <% } %>
                </div>
            </main>
        </div>
        <script src="/js/main.js"></script>
    </div>
</body>
</html>